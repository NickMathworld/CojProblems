#include <bits/stdc++.h>
#define MAXN (1<<17)
#define lld long long

lld dp(lld mask,lld n){

}
class MASK{
	private:
		lld mask;
	public:	
		lld getmask(){
			return mask;
		}
		lld getNBitIndex(lld n){
			lld 
		}
};